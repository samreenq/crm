<div class="form-group">
                    <button type="{{$type}}" class="btn {{$buttonClass}}" name="{{$name }}"
                    @if(!empty($name)) name="{{$name}}"  @endif >{{$buttonValue}}</button>
 </div>